# Add the new parameters [here](tests/)
tests:
  storage:
    throughput: 
      fileNumber: 10
      fileSize: 1M
    checksum:
      fileNumber: 10
      fileSize: 1M
    statFile:
      filepath: "eos/user/u/user2/"

  jupyterhub_api:
    webReachable:
      port: '443'
    session:
      port: '443'
      users: 'user2'
      path: 'srv/jupyterhub'
    active_session:
      port: '443'
      users: 'user2'
      path: 'srv/jupyterhub'
    stop_session:
      port: '443'
      users: 'user2'
      path: 'srv/jupyterhub'

